# Copy to .env.local only when connecting to a real WooCommerce backend.
# If these values are absent, the app automatically runs in mock mode.

NEXT_PUBLIC_WC_URL=
WC_CONSUMER_KEY=
WC_CONSUMER_SECRET=
